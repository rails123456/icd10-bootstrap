<h1>Listing specialties</h1>

<table>
  <tr>
    <th>Sp code</th>
    <th>Sp name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @specialties.each do |specialty| %>
  <tr>
    <td><%= specialty.sp_code %></td>
    <td><%= specialty.sp_name %></td>
    <td><%= link_to 'Show', specialty %></td>
    <td><%= link_to 'Edit', edit_specialty_path(specialty) %></td>
    <td><%= link_to 'Destroy', specialty, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Specialty', new_specialty_path %>
